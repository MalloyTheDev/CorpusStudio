{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the main window — lets the React frontend invoke the platform_* commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
