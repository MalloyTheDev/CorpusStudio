{
  "name": "corpus-studio-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Corpus Studio's Tauri 2 + React shell — a client over the platform's language-neutral contracts. Renders the run lifecycle (profile → plan → fit → run) from the same JSON-Schema contracts the engine emits, skinned with the shared Nocturne design tokens (see docs/design/).",
  "scripts": {
    "gen:contracts": "node scripts/gen-contracts.mjs",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@phosphor-icons/react": "^2.1.7",
    "@tauri-apps/api": "^2.1.1",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "json-schema-to-typescript": "^15.0.3",
    "typescript": "^7.0.2",
    "vite": "^8.1.5"
  }
}
